import { fileURLToPath } from 'node:url'; import { defineConfig } from 'vite'; import react from '@vitejs/plugin-react'; export default defineConfig({ envDir: fileURLToPath(new URL('../..', import.meta.url)), plugins: [react()], server: { host: '0.0.0.0', port: 3333, allowedHosts: [ 'localhost', '127.0.0.1' ] }, preview: { host: '0.0.0.0', port: 3333, allowedHosts: [ 'localhost', '127.0.0.1' ] } }); 